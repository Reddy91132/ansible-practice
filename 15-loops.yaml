- name: loops demo
  hosts: web
  become: yes
  tasks:
  - name: print the names
    ansible.builtin.package:
     msg: "{{ items }}"
      state: present

    loop: 

    - mysql
    - nginx
    - postfix
    - httpd 
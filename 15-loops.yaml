- name: loops demo
  hosts: web
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.package:
      name: "{{ items }}"
      state: present

    loop: 

    - mysql
    - nginx
    - postfix
    - httpd 